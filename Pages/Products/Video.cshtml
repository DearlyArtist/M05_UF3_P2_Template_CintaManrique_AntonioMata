@page
@model M05_UF3_P2_Template.Pages.Products.VideoModel
@{
        if (Model.Id > 0)
    {
        ViewData["Title"] = "Video - " + Model.product.Id;
    }
    else
    {
        ViewData["Title"] = "Video - Create";
    }
}

<form method="post">
    <button type="submit">Save</button>

    <div class="card">
        <div class = "card-header">
            <h1 class = "card-title">
                @if (Model.Id > 0)
                {
                    <img class="img img-thumbnail img-fluid" src="@Model.product.Product_Id" style="width: 200px; height: 100px; object-fit: scale-down; background-color:@Model.product.Duration;" />
                    <span>@Model.product.Product_Id - <small>Edit product</small></span>
                }
                else
                {
                    <span>Create Video</span>
                }
                <button type="submit" class="btn btn-primary float-right"><span class="material-icons">save</span></button>
            </h1>

        </div>
        <div class = "card-body">
            <div class="form-row">
                <div class="form-group col-md-2">
                    <label for="Video_Id_Input col-sm-2 col-form-label"><span class="material-icons mx-1" style="font-size: 1rem;">tag</span>Id</label>
                    @{
                        string CustomId = Model.Id > 0 ? Model.Id.ToString() : null;
                    }
                     <input type="text" asp-for="product.Id" value="@CustomId" readonly class="form-control" id="Product_Id_Input" placeholder="-">
                </div>
                <div class="form-group col-md-2">
                    <label for="Video_Duration_Input col-sm-2 col-form-label"><span class="material-icons mx-1" style="font-size: 1rem;">badge</span>Duration</label>
                    <input type="text" asp-for="product.Duration">
                </div>
            </div>

        </div>
    </div>
</form>